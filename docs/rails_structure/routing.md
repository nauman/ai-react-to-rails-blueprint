# Routing (shadow plan)

- Map user-facing paths to controllers/actions and Phlex views.
- Note nested resources, pagination params, and tag filter params.
- Call out Turbo Stream endpoints (if any) for partial updates.
