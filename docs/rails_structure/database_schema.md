# Database Schema (shadow plan)

- Derive tables and columns from `schema.json`; include types, nullability, indexes, and foreign keys.
- Include acts-as-taggable-on tables and join tables as needed.
- Note pagination-related indexes (e.g., created_at or id for Pagy ordering).
